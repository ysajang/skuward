# Database
DATABASE_URL="postgresql://skuward:skuward_dev_2026@localhost:5432/skuward"

# Shopify (auto-populated by shopify app dev)
SHOPIFY_API_KEY=""
SHOPIFY_API_SECRET=""
SCOPES="read_products,write_products,read_inventory,write_inventory"
SHOPIFY_APP_URL=""
# App handle (the URL slug, e.g. "skuward"). Used to build the post-billing
# returnUrl so the embedded session is restored after charge approval.
# Find it in shopify.app.toml (handle = "...") after `shopify app dev`/`deploy`.
SHOPIFY_APP_HANDLE=""
